# Runtime dependencies, pinned so a rebuilt container matches a tested one.
fastapi==0.141.1
uvicorn[standard]==0.52.3
pydantic==2.13.4
pydantic-settings==2.15.0
PyJWT==2.13.0
python-multipart==0.0.20

# Note: numpy / opencv are intentionally absent. The API is a thin control
# plane over SQLite; frame decoding and inference belong to the `ml` service,
# which carries those dependencies.
